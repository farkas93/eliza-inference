# tts

OpenAI-compatible text-to-speech endpoint backed by Piper.

Default API:

```text
POST /v1/audio/speech
```

Start:

```bash
./scripts/start tts --profile tts/piper-lessac
./scripts/smoke-test tts
```
