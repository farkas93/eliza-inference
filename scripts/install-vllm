#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck disable=SC1091
source "$ROOT_DIR/scripts/lib/common.sh"
load_env

INSTALL_VLLM="true"
VLLM_PACKAGE="vllm"
UV_TORCH_BACKEND="auto"
EXTRA_UV_PIP_ARGS=()

while [[ $# -gt 0 ]]; do
  case "$1" in
    --no-install)
      INSTALL_VLLM="false"
      shift
      ;;
    --pre)
      EXTRA_UV_PIP_ARGS+=(--pre)
      shift
      ;;
    --package)
      VLLM_PACKAGE="${2:-}"
      shift 2
      ;;
    --torch-backend)
      UV_TORCH_BACKEND="${2:-}"
      shift 2
      ;;
    --index-url)
      EXTRA_UV_PIP_ARGS+=(--index-url "${2:-}")
      shift 2
      ;;
    --extra-index-url)
      EXTRA_UV_PIP_ARGS+=(--extra-index-url "${2:-}")
      shift 2
      ;;
    -h|--help)
      cat <<USAGE
Usage: ./scripts/install-vllm [options]

Installs and verifies vLLM for GPU serving.

Options:
  --no-install              Skip installation and only run verification.
  --pre                     Allow pre-release packages.
  --package PACKAGE         vLLM package spec to install. Default: vllm
  --torch-backend BACKEND   uv torch backend. Default: auto
  --index-url URL           Pass an index URL to uv pip install.
  --extra-index-url URL     Pass an extra index URL to uv pip install.
USAGE
      exit 0
      ;;
    *)
      echo "Unknown argument: $1" >&2
      exit 2
      ;;
  esac
done

require_uv() {
  if ! command -v uv >/dev/null 2>&1; then
    echo "uv is required. Run ./scripts/install first." >&2
    exit 1
  fi
}

show_host_gpu() {
  if command -v nvidia-smi >/dev/null 2>&1; then
    echo "Host GPU:"
    nvidia-smi --query-gpu=name,driver_version,memory.total --format=csv,noheader || true
  else
    echo "[warn] nvidia-smi is not available; GPU verification will rely on torch only" >&2
  fi
}

install_vllm() {
  echo "Syncing base project dependencies"
  uv sync --project "$ROOT_DIR"

  if [[ "$INSTALL_VLLM" == "true" ]]; then
    echo "Installing vLLM package: $VLLM_PACKAGE"
    uv pip install --project "$ROOT_DIR" -U "$VLLM_PACKAGE" --torch-backend="$UV_TORCH_BACKEND" "${EXTRA_UV_PIP_ARGS[@]}"
  else
    echo "Skipping vLLM install (--no-install)."
  fi
}

verify_python_runtime() {
  uv run --project "$ROOT_DIR" --no-sync python - <<'PY'
from __future__ import annotations

import importlib.metadata as md
import platform
import shutil
import subprocess
import sys

print("Python runtime:")
print(f"  executable={sys.executable}")
print(f"  version={sys.version.split()[0]}")
print(f"  platform={platform.platform()}")
print(f"  machine={platform.machine()}")

print("\nPackage versions:")
for package in ("torch", "vllm", "transformers", "tokenizers", "triton", "xformers"):
    try:
        print(f"  {package}={md.version(package)}")
    except md.PackageNotFoundError:
        print(f"  {package}=not-installed")

try:
    import torch
except Exception as exc:  # pragma: no cover - install-time diagnostic
    raise SystemExit(f"Failed to import torch: {exc}") from exc

print("\nTorch CUDA:")
print(f"  torch.version.cuda={torch.version.cuda}")
cuda_available = torch.cuda.is_available()
print(f"  torch.cuda.is_available={cuda_available}")
if not cuda_available:
    raise SystemExit("torch CUDA is not available; vLLM is not ready for GPU serving")

device_count = torch.cuda.device_count()
print(f"  torch.cuda.device_count={device_count}")
for index in range(device_count):
    props = torch.cuda.get_device_properties(index)
    print(f"  device.{index}.name={props.name}")
    print(f"  device.{index}.capability={props.major}.{props.minor}")
    print(f"  device.{index}.total_memory_gb={props.total_memory / 1024**3:.2f}")

try:
    import vllm
except Exception as exc:  # pragma: no cover - install-time diagnostic
    raise SystemExit(f"Failed to import vllm: {exc}") from exc

print("\nvLLM import: ok")
vllm_cli = shutil.which("vllm")
print(f"vllm CLI on PATH: {vllm_cli or 'not-found-in-shell-path'}")

try:
    completed = subprocess.run(
        [sys.executable, "-m", "vllm.entrypoints.openai.api_server", "--help"],
        check=False,
        stdout=subprocess.DEVNULL,
        stderr=subprocess.DEVNULL,
        timeout=30,
    )
    print(f"vLLM OpenAI server module help exit_code={completed.returncode}")
except Exception as exc:
    print(f"vLLM OpenAI server module help check failed: {exc}")
PY
}

verify_vllm_cli() {
  if uv run --project "$ROOT_DIR" --no-sync vllm --help >/dev/null 2>&1; then
    echo "[ok] vllm CLI works through uv run --no-sync"
  else
    echo "[warn] vllm CLI did not run through uv --no-sync; module-based serving may still work" >&2
  fi
}

require_uv
show_host_gpu
install_vllm
verify_python_runtime
verify_vllm_cli

cat <<'EOF'


Next low-risk serving check:
  ./scripts/start voice-llm --profile voice-gemma4-e4b-vllm-text
  ./scripts/smoke-test voice-llm --profile voice-gemma4-e4b-vllm-text

Then test Qwen conservatively:
  ./scripts/start qwen-coder --profile qwen-shared-128k
  ./scripts/smoke-test qwen-coder --profile qwen-shared-128k
EOF
